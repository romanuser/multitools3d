Aviso de "Adicionar a tela de inicio" no celular -- estava faltando por completo.

Este pacote tinha ficado de fora nas aplicacoes anteriores. Contem:

- src/components/install-prompt.tsx  (NOVO)
- src/app/manifest.ts                (NOVO)
- src/app/layout.tsx                 (SUBSTITUI o seu -- e o seu arquivo atual, so
                                       com o manifest/icones/InstallPrompt acrescentados)
- public/icon-192.png                (NOVO)
- public/icon-512.png                (SUBSTITUI o icone antigo, laptop cinza, pelo
                                       novo, verde, com o simbolo de camadas)
- public/apple-touch-icon.png        (NOVO)
- public/favicon-32.png              (NOVO)

COMO APLICAR (pra nao duplicar pasta de novo)
De dentro da pasta onde voce extraiu este zip, rode:

    Copy-Item -Path "src\*" -Destination "C:\Users\User\Desktop\WEBDEV Studio\SITES\multitool3d\src" -Recurse -Force
    Copy-Item -Path "public\*" -Destination "C:\Users\User\Desktop\WEBDEV Studio\SITES\multitool3d\public" -Recurse -Force

Troque o caminho de destino se a pasta do projeto nao estiver exatamente ai.

DEPOIS DE COPIAR, confirme que nao apareceu pasta duplicada:

    Get-ChildItem -Directory
    (deve mostrar so: .netlify, .next, node_modules, public, src -- nada de "app"
    ou "components" ou "src" soltos)

Depois, commit + push como sempre.

COMO TESTAR DEPOIS DO DEPLOY
- No iPhone (Safari): abra o site, espere uns 3 segundos -- o aviso deve aparecer
  sozinho, no rodape da tela.
- No Android (Chrome): pode nao aparecer na primeira visita -- isso e o proprio
  Chrome decidindo, nao e bug. As vezes precisa visitar de novo ou usar por um
  tempinho antes dele oferecer.
